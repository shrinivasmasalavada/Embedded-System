int ldr = 34;
int led = 2;

void setup() {
  pinMode(led, OUTPUT);
  Serial.begin(115200);
}

void loop() {
  int value = analogRead(ldr);

  if(value < 2000) {
    digitalWrite(led, HIGH);
  } else {
    digitalWrite(led, LOW);
  }

  Serial.println(value);
  delay(500);
}